# Python virtualenv
.venv/
__pycache__/
*.pyc

# Match data + runtime state (regenerated each session)
match_data.json
match_data.csv
control.json
status.json
reports/
*.tmp

# Vision pipeline artifacts (regenerated / downloaded on demand)
# YOLO model weights, sample/match videos, pipeline + bridge output,
# ultralytics training runs, and downloaded training datasets.
*.pt
*.mp4
match_stats*.json
match_data.vision*.json
runs/
datasets/
soccer_frames*/
annotation_frames/
match_segments/
*.zip

# macOS
.DS_Store
